{
  "private": true,
  "name": "github-actions-test",
  "packageManager": "yarn@3.1.0",
  "workspaces": [
    "packages/app"
  ],
  "scripts": {
    "build:commmon": "docker build -t keonwoo/test:default -f Dockerfile.workflow . && docker push keonwoo/test:default"
  }
}
